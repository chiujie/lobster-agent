services:
  - type: web
    name: lobster-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 4 --worker-class sync
    envVars:
      - key: OPENAI_API_KEY
        sync: false
